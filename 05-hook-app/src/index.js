import React from 'react';
import ReactDOM from 'react-dom';
import { HooApp } from './HooApp';

ReactDOM.render(
  <React.StrictMode>
    <HooApp />
  </React.StrictMode>,
  document.getElementById('root')
);